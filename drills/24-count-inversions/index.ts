/**
 * Count Inversions
 * 
 * Count the number of inversions in an array. An inversion is a pair (i, j)
 * where i < j and arr[i] > arr[j]. Use merge sort approach.
 */

export function countInversions(arr: number[]): number {
  // TODO: Implement count inversions using merge sort approach
  return 0;
}

