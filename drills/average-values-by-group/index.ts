/**
 * Average Values By Group
 * 
 * Given an array of objects with 'category' and 'value' properties,
 * return an object where each category maps to the average value for that category.
 * Use spread/rest operators where appropriate.
 */

export interface CategoryValue {
  category: string;
  value: number;
}

export function averageValuesByGroup(arr: CategoryValue[]): Record<string, number> {
  // TODO: Implement this function
  return {};
}

