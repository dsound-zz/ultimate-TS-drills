/**
 * Minimum Window Substring
 * 
 * Given two strings s and t, return the minimum window substring of s such that
 * every character in t (including duplicates) is included in the window.
 * Return empty string if no such window exists. Use sliding window technique.
 */

export function minimumWindowSubstring(s: string, t: string): string {
  // TODO: Implement sliding window solution
  return '';
}

